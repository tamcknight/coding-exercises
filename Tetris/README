

This program is written in python.  It requires python 3.x to run.  It is setup to be run on linux or macos.

To install python there are several ways to do this.

1) on mac you can use brew: https://www.python.org/downloads/
2) on linux you can use: sudo apt-get install python3.6

Now make sure python is on your path.  to set this:

export PATH=$PATH:PATH_TO_PYTHON_EXECUTABLE/python

then to run
cd scripts; ./tetris PATH_TO_INPUT_FILE > output.txt

for example:
cd scripts; ./tetris ../data/input.txt > output.txt